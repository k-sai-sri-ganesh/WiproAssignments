package com.example.gani.swagger;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    @Bean
    public OpenAPI notificationApi() {
        return new OpenAPI()
                .info(new Info().title("Notification API").version("1.0"));
    }
}
